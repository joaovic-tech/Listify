import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Listify | Seu gerenciado de tarefas",
};

export default function Home() {
  return <h1>Olá shadcn-ui!</h1>;
}
